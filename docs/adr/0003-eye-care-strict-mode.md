# ADR 0003: 护眼严格模式覆盖所有显示器

## 状态

已接受

## 背景

护眼提醒的"严格模式"旨在强制用户停止工作、休息眼睛。若弹窗仅显示于单屏，用户可将鼠标移往他屏继续操作，使提醒失效。

## 决策

严格模式下，在所有连接的显示器上显示全屏半透明遮罩，护眼弹窗居中于当前活动显示器。倒计时结束前不可关闭。

温和模式与锁时模式仅在主显示器居中显示弹窗。

## 后果

- 正面：严格模式名副其实，真正阻断工作流。
- 负面：实现复杂度增加，需枚举所有显示器并创建对应窗口。
- 负面：会议或演示场景可能尴尬，故提供全局勿扰模式作为补偿。
